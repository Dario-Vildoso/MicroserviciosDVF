<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Resultado</title>
</head>
<body>
    <h1>Resultado</h1>
    <p>
        El resultado de <%= a %> 
        <% if (operacion === 'sumar') { %>+<% } %>
        <% if (operacion === 'restar') { %>-<% } %>
        <% if (operacion === 'multiplicar') { %>*<% } %>
        <% if (operacion === 'dividir') { %>/<% } %>
        <%= b %> es <%= resultado %>
    </p>
    <a href="/">Volver</a>
</body>
</html>
